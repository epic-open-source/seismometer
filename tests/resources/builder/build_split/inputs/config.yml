# Examples:
# seismometer extract --file ../workspace/seismometer/tests/resources/builder/gengeology.ipynb
# seismometer build
other_info: 
  template: "binary"
  info_dir: "more"
  usage_config: "more/usage_config.yml"
  event_definition: "data_dict.yml"
  prediction_definition: "data_dict.yml"
  data_dir: "."
  prediction_path: "scores.parquet"
  event_path: "events.parquet"
  metadata_path: "metadata.json"
